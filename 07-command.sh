#!/bin/bash

DATE=$(date +%F)

echo "Today's Date is : $DATE"

# COMMAND - we can keep the command inside $() and the output of the command will be stored in the variable. we can use that variable where ever we want.